import React from "react";
import s from "./Footer.module.scss";

const Footer = () => {
  return (
    <div className={s.footer}>
      <span className={s.footerLabel}>2025 &copy; Urban Analytics Dashboard — Tashkent Environmental Data</span>
    </div>
  )
}

export default Footer;
